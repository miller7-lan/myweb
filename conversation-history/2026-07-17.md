# 2026-07-17 对话摘要

## 01:36 — Add Mailweek to portfolio

### 目标

- 将 Mailweek macOS CLI 补充到星系作品集，保持现有视觉语言与信息准确度。

### 执行要点

- 新增完整的 Creations 项目档案、引导条目与 Orbit v0.3.2 发行/源码记录。
- 区分外部源码入口与可下载制品，避免把源码链接伪装成下载。
- 修复 Orbit 已挂载时从项目详情跳转不能选中目标发行项的问题。

### 使用的工具

- `galaxy-site`：构建、检查发布目录、验证正式站点并在自动部署延迟时执行受控手动发布。
- `Playwright`：验证从 Creations 打开 Mailweek 并跳转到对应 Orbit 节点的真实交互。
- `npm` 校验脚本：执行 lint、生产构建、作品集内容断言与联网链接检查。

### 完成与效果

- 完成状态：完成。
- 提交 `6127a2b` 已推送至 `origin/main`。
- 正式站点返回 HTTP 200，线上资源已包含 Mailweek、P0–P4 登记簿、4B→9B 复核、v0.3.2 与正确源码地址。
- 现有 vendor chunk 体积提示仍为非阻断警告，本次未引入新的构建错误。

### 变更文件

- `package.json`：增加作品集内容验证命令。
- `scripts/verify-downloads.mjs`：正确处理外部源码入口。
- `scripts/verify-portfolio-content.mjs`：新增 Mailweek 关键内容断言。
- `src/components/Themes/CreationsContent.tsx`：新增 Mailweek 项目档案。
- `src/components/Themes/OrbitContent.tsx`：改进源码入口与目标发行跳转。
- `src/data/guide.ts`：补充 Mailweek 操作引导。
- `src/data/releases.ts`：新增 Mailweek v0.3.2 Orbit 记录。

### 决策与约束

- 保持作品集现有 HUD/星系视觉体系，不修改全局视觉系统。
- Mailweek 仅提供源码入口，不宣称存在未提供的安装包。
- 自动部署持续返回旧资源后，按项目规范使用手动发布回退。

### 待办与风险

- 无。


---

## 02:32 — 发布 Mailweek DMG 并修复跳转

### 目标

- 将 Mailweek 公开入口由源码链接改为真实可下载的 macOS DMG，并修复项目详情跳转到错误 Orbit 节点的问题。

### 执行要点

- 将 Mailweek v0.3.2 冻结为独立 Apple Silicon 二进制，组装可启动 Terminal 的 Mailweek.app，并生成 DMG。
- 移除所有面向访客的 Mailweek 源码入口，改为带实际体积和 SHA-256 的主下载项。
- 复现严格模式下状态初始化执行两次导致跳转目标被提前删除的问题；将清理动作移到挂载后的 effect。
- 自动部署持续返回旧资源后，按项目规范使用手动 Cloudflare Pages 发布回退。

### 使用的工具

- `PyInstaller` 与 `hdiutil`：生成并验证独立 Apple Silicon DMG。
- `Playwright`：在本地和正式站验证 Creations → Mailweek → Orbit Node 05 的真实跳转。
- `galaxy-site`：构建、检查发布目录并执行受控手动部署。
- `Mailweek benchmark`：用 DMG 同源二进制运行 11 项合成准确度套件。

### 完成与效果

- 完成状态：完成。
- DMG 大小 19,562,241 字节，SHA-256 为 ea9da94b1e77ec2db64c1117cc60f52ace60648b009ebb0d747c01f85207dea4。
- Ruff、mypy、68 个测试、作品集 lint/build、12 个联网下载检查全部通过。
- qwen3.5:9b 合成质量套件有效输出 11/11、期望命中 11/11、match_rate 1.0。
- 正式站跳转后 Mailweek Node 05 准确选中，并显示 DMG 下载与正确校验值。
- 发布提交为 b04a4e8，已推送至 origin/main。

### 变更文件

- `release-assets/Mailweek-0.3.2-macOS.dmg`：新增可下载的 Apple Silicon DMG。
- `src/data/releases.ts`：以 DMG 主下载替换源码入口，并补充准确系统要求。
- `src/components/Themes/OrbitContent.tsx`：修复严格模式跳转目标丢失并恢复纯下载行为。
- `src/data/guide.ts`：移除源码引导，改为 DMG 下载引导。
- `scripts/verify-downloads.mjs`：所有入口重新要求 SHA-256。
- `scripts/verify-portfolio-content.mjs`：要求 DMG 且禁止 Mailweek 源码入口。

### 决策与约束

- DMG 内含独立运行环境，不要求另装 Python；仍准确要求 Apple Silicon、macOS 11+ 和本机 Ollama。
- 网站暂不公开 Mailweek 源码。
- 下载资产通过 GitHub release-assets 提供，Pages 仅承载页面。

### 待办与风险

- DMG 使用临时签名而非 Apple Developer ID 公证；首次打开可能需要右键选择“打开”。
